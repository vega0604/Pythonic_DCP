from setuptools import setup, find_packages

setup(
    name="pdcp",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "dcp",  # Add any other dependencies your package needs
    ],
) 